#version 430 core
layout(local_size_x = 8, local_size_y = 8, local_size_z = 8) in;
layout(r32ui, binding = 0) uniform uimage3D u_brickPool;

void main() {
    imageStore(u_brickPool, ivec3(gl_GlobalInvocationID.xyz), uvec4(0, 0, 0, 0));
}
