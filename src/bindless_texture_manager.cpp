#include "bindless_texture_manager.h"

namespace Boidsish {
    // Empty for now as implementation is in the header for simplicity/singleton.
    // This file ensures the symbols are linked if we move implementation here later.
}
